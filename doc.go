// Package gogd is a native Go implementation of the image operations
// exposed by PHP's gd extension, built on top of Go's standard image
// packages. It uses no cgo and does not link against libgd.
package gogd
